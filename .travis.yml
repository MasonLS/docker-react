language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t masonseale/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true masonseale/docker-react npm run test